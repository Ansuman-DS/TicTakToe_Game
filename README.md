🎮 TicTacToe Game – Java Console Project

This is a simple Java-based Tic-Tac-Toe game built using object-oriented programming concepts. It runs in the console and allows two players (X and O) to play by entering row and column numbers. The game handles invalid inputs, checks winning conditions, detects draws, and displays the board after every move.

✅ Features

3×3 Tic-Tac-Toe board

Two-player turn-based gameplay (X vs O)

Input validation (valid positions only)

Win detection (rows, columns, diagonals)

Draw detection when the board is full

Clear, console-friendly board display

Organized OOP structure with:

TicTacToeGame

GameBoard

GameState

Player

🛠 Technologies Used

Java

OOP Concepts

Scanner (for user input)

📌 How to Run

Install Java (JDK 8 or above).

Copy all .java files into the same folder.

Compile the program:

javac TicTacToeGame.java


Run the game:

java TicTacToeGame

📂 Project Structure
TicTacToeGame.java     → Main game logic
GameBoard.java         → Board structure & move handling
Player.java            → Player enum (X/O)
GameState.java         → Game state enum

📝 Short Description

A simple Java console Tic-Tac-Toe game using OOP principles where two players enter row-column positions to play. It includes win checking, draw detection, and a clean interactive board display.
